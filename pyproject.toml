[project]
name = "uv-python-version-pin-probe"
version = "0.1.0"
description = "Minimal uv probe: Python version pinning via .python-version and requires-python"
requires-python = "==3.10.2"
dependencies = [
    "requests==2.31.0",
    "click==8.1.7",
]

[tool.uv]
# No extra uv-specific config needed for this pattern